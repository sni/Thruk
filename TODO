Todo List:

- create models for hosts / services / contacts
- reload cgi.cfg if changed
- is_authorized_for_read_only in Catalyst::Authentication::Store::FromCGIConf
- * in the cgi.cfg
- no commands if authorization is disabled
- overload role authentication ( to add host/service role checks )
- add authorization part to all querys
- fix pending state on host/service details page (should be just a empty message)
- verify macros is action_url, icon_url and notes_url
- finish performance data on extinfo type 4
- config.cgi
- reports / logs

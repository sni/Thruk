Thruk Development Guide
=======================

Unsorted things you may want to do during development:


Install a Clean Nagios From Source
----------------------------------

-----
use this command line to setup a test nagios installation

export TARGET_DIR=/Users/sven/projects/nagios/nagios3.2.0
./configure --prefix=$TARGET_DIR \
            --with-nagios-user=`whoami` \
            --with-nagios-group=`groups | awk '{ print $1 }'` \
            --with-init-dir=$TARGET_DIR/init.d \
            --with-httpd-conf=$TARGET_DIR/apache.conf.d/
make all && make install
-----


Generate a Test Configuration
-----------------------------

you may use Nagios::Generator::TestConfig for this step


Logo
----
Logo has been rendered with
http://cooltext.com/
Text Color: 414141
BG: FFFFFF
Font: Futuristic Fonts -> Oscilloscope 4
Size: 90


Setup Package Build Environment
-------------------------------

----
1.  install debian netinst
      - minimal inst, create user thruk
2.  add "contrib non-free" to /etc/apt/sources.list
3.  #> apt-get install libapache2-mod-fastcgi libfile-copy-recursive-perl libmodule-install-perl \
          make gcc git git-core libgd2-xpm-dev
4.  #> su - thruk
5.  %> cd /tmp
6.  %> wget http://search.cpan.org/CPAN/authors/id/A/AP/APEIRON/local-lib-1.004009.tar.gz
7.  %> tar zxvf local-lib-1.004009.tar.gz
8.  %> cd local-lib-1.004009
9.  %> eval $(perl -I$HOME/perl5/lib/perl5 -Mlocal::lib)
       export PERL5LIB="$HOME/perl5/lib/perl5'
       export PERL_MM_OPT='INSTALL_BASE=$HOME/perl5
       INSTALLARCHLIB=$HOME/perl5/lib/perl5
       INSTALLSITEARCH=$HOME/perl5/lib/perl5
       INSTALLVENDORARCH=$HOME/perl5/lib/perl5"
10. %> perl Makefile.PL
11. %> make install
12. %> cd && git clone http://github.com/sni/Thruk.git
13. %> cd Thruk
14. %> ./script/thruk_create_package.pl
----
